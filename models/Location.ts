/**
 * @file Declares Location data type representing the 
 * latitude and longitutde coordinates of a user
 * on Tuiter
 */
export default interface Location {
	latitude: number,
	longitude: number
};